[pytest]
# app/smoke_test.py is the manual smoke script documented in the README, not a
# pytest suite: importing it pulls in keyboard, torch and the Whisper stack. Left
# collectable, `pytest` in a fresh clone dies with a collection error instead of
# reporting no tests.
norecursedirs = app
